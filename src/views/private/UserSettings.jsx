import React from "react";
import { ContainerLayout, Settings } from "../../components";

const UserSettings = () => {
  return (
    <ContainerLayout>
      <Settings />
    </ContainerLayout>
  );
};

export default UserSettings;
